server:
  port: 8084

spring:
  application:
    name: PARKING-SPACE-SERVICE

  datasource:
    url: jdbc:mysql://localhost:3306/parking_space_db
    username: root
    password: harshima@147
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

  config:
    import: optional:configserver:http://localhost:9296

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
